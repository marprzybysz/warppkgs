pkgname=unzip
pkgver=6.0
description="ZIP archive extraction tool — makedep dla Kotlin, Groovy, zen-browser"
arch=x86_64
license=custom
deps=glibc
makedeps=gcc,make
source=https://sourceforge.net/projects/infozip/files/UnZip%206.x%20%28latest%29/UnZip%206.0/unzip60.tar.gz
sha256=VERIFY

build() {
    make $MAKEFLAGS -f unix/Makefile generic
}

package() {
    make prefix=/usr DESTDIR="$DESTDIR" -f unix/Makefile install
}
