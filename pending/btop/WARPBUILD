pkgname=btop
pkgver=1.4.0
description="Resource monitor with a modern terminal UI"
arch=x86_64
license=Apache-2.0
deps=glibc,gcc-runtime
makedeps=gcc,g++,make
source=https://github.com/aristocratos/btop/archive/refs/tags/v1.4.0.tar.gz
sha256=

build() {
    make PREFIX=/usr OPTFLAGS="" $MAKEFLAGS
}

package() {
    make PREFIX=/usr DESTDIR="$DESTDIR" install
}
