pkgname=zoxide
pkgver=0.9.6
description="Inteligentny zamiennik cd — zapamiętuje najczęściej odwiedzane katalogi"
arch=x86_64
license=MIT
deps=glibc
makedeps=rust
source=https://github.com/ajeetdsouza/zoxide/archive/refs/tags/v0.9.6.tar.gz
sha256=VERIFY

build() {
    cargo build --release --locked
}

package() {
    install -Dm755 target/release/zoxide "$DESTDIR/usr/bin/zoxide"
}
