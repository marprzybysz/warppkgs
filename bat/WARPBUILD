pkgname=bat
pkgver=0.24.0
description="cat z podświetlaniem składni, numerami linii i integracją z git"
arch=x86_64
license=MIT
deps=glibc
makedeps=rust
source=https://github.com/sharkdp/bat/archive/refs/tags/v0.24.0.tar.gz
sha256=VERIFY

build() {
    cargo build --release --locked
}

package() {
    install -Dm755 target/release/bat "$DESTDIR/usr/bin/bat"
}
