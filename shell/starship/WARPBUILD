pkgname=starship
pkgver=1.21.1
description="Minimal, fast, and customizable cross-shell prompt"
arch=x86_64
license=ISC
deps=glibc,gcc-runtime
makedeps=rust
source=https://github.com/starship/starship/archive/refs/tags/v1.21.1.tar.gz
sha256=

build() {
    cargo build --release
}

package() {
    install -Dm755 target/release/starship "$DESTDIR/usr/bin/starship"
}
